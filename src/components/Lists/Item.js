import styled from 'styled-components';

export const Item = styled.li`
  color: #525CDD;
  text-transform: uppercase;
  letter-spacing: 1px;
  font-size: 0.8em;
  margin: 5px;
`;

export default Item